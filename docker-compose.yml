version: '3'

services:
  ismg_landkreis_karte:
    build: .
    container_name: ismg_landkreis_dashboard
    volumes:
      - .:/app
    working_dir: /app
    ports:
      - "3840:3838"
    restart: always
    networks:
      - landkreis_network

networks:
  landkreis_network:
    external: false
